/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // If your repository name is 'my-repo', set basePath to '/my-repo'
    basePath: '/peerlist-formbuilder',
};

export default nextConfig;
